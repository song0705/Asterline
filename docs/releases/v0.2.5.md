# Asterline v0.2.5

This release includes the v0.2.4 reliability and security improvements, plus a
Debian packaging repair discovered during release validation.

## Highlights

- Workspace state, generated skills, locks, and imported transcripts reject
  unsafe symbolic-link and traversal paths instead of following them outside
  the selected workspace.
- Streaming messages, reasoning, tool progress, and errors retain their
  canonical state under UI backpressure. Logs and rendered history have
  explicit byte budgets so a noisy process cannot make the TUI grow without
  bound.
- Cancelling a pending native attach safely releases a late reservation, and
  model discovery no longer repeatedly blocks the UI while catalog data is
  loading.

## Distribution and release integrity

- Debian package dependency discovery now runs from a valid temporary Debian
  source tree. The release validates `amd64` and `arm64` packages with install,
  execution, and removal smoke tests on Debian 12 and Ubuntu 24.04.
- macOS assets are Developer ID signed and notarized when credentials are
  configured; this release is intentionally unsigned and unnotarized, while
  retaining checksums and GitHub artifact attestations. Linux archives are
  checked for their glibc 2.28 baseline and bundled SQLite before publication.
- GitHub Release assets are uploaded in platform groups: macOS, GNU/Linux and
  Debian packages, then Windows ZIP and Setup. `SHA256SUMS` uses that same
  deterministic order.

## 中文摘要

- 本版本包含 v0.2.4 的可靠性与安全性改进，并修复了发布验证中发现的 Debian 打包问题。
- Debian 包的依赖推导现在会在有效的临时 Debian 源码树中运行；发布会在 Debian 12 和
  Ubuntu 24.04 上完成 `amd64` / `arm64` 安装、执行和卸载验证。
- 工作区状态、自动生成的 Skill、锁文件和导入转录会拒绝不安全的符号链接与路径穿越；UI
  背压下仍会保留消息、思考、工具进度和错误的规范状态，日志和渲染历史也有明确字节上限。
- 配置 macOS 凭据时会进行 Developer ID 签名和公证；本版本按授权以未签名、未公证的
  形式发布，但仍提供校验和和 GitHub artifact attestation。发布资产按 macOS、
  Linux/Debian、Windows 分组上传。
