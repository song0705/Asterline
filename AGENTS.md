# Asterline project instructions

## Verification

- 当用户咨询事实、状态或外部信息时，先搜索或验证；不要凭空猜测。

## Release tags

- 版本 tag 是发布承诺，不是 CI、打包或跨平台冒烟测试的触发器。
- 必须先在未打 tag 的提交上完成所有发布预检：构建、目标平台打包、安装/冒烟测试、资产集合与校验验证；全部成功后，才可以创建并推送版本 tag。
- 预检失败时，禁止创建或推送 tag，也禁止通过打下一个版本 tag 来绕过失败或造成无意义跳号。先修复并重新完成预检。
- 若误推了尚未发布的失败 tag：立即停止，向用户说明原因；仅在用户明确授权后删除该 tag。不得保留失败 tag，也不得未经用户同意直接跳到新版本。
